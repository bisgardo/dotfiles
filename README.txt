# Dotfiles

Custom setup of Bash and Git with other kinds of dotfiles coming later.

## Install

* Clone dotfiles into a directory of choice: `git clone ...`.

* Run install script `dotfiles/install.sh`.
